import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#BF55EC",
    },
    secondary: {
      main: "#EEEEEE",
    },
    background: {
      default: "#FCF9F0",
    },
  },
});

export default theme;
